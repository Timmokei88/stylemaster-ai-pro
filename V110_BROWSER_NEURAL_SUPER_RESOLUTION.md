# MixoLabs V110 — Browser Neural Super-Resolution

- Replaces canvas-only enlargement as the primary enhancer with genuine neural super-resolution.
- Uses TensorFlow.js and UpscalerJS's pretrained default super-resolution model in the customer’s browser.
- A 2× enhancement runs one neural pass; 4× runs two neural 2× passes.
- Patch-based processing limits peak device memory and keeps images on the customer’s device.
- No Gemini image-generation request is made and no MixoLabs credit is debited.
- The model downloads on first use and is normally cached by the browser.
- Natural, balanced and strong finishing controls apply after neural enhancement.
- The prior local multi-pass processor remains only as an offline fallback when the neural model cannot load.
- Generation remains Gemini 3.1 Flash Lite at 1K and one credit.
- No billing, moderation, subscription, generation, scrolling or account code was changed.
