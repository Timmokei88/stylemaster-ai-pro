# V103 — Clean One-Image Default

- Changes the real HTML source default from six images to one image.
- Changes both initial Generate buttons and the initial credit preview to one image and one 1K credit.
- Prevents the browser from restoring an old six-image form value after refresh.
- Reapplies the one-image default when a page is restored from the browser back/forward cache.
- Customers can still deliberately choose two through six images after the page loads.
- Preserves the V102 native-scroll, provider-compatible family-photo generation, refund, subscription and account fixes.
